Keyword: FIFO watermark
Occurrences: 4
================================================================================

Page   22: the FIFO and Data registers); (3) accelerometer event interrupts; (4) FIFO watermark; (5) FIFO overflow. The interrupt status can be
Page   66: Trigger FIFO watermark interrupt on every ODR (DMA write) if
Page   66: Lower bits of FIFO watermark. Generate interrupt when the FIFO reaches
Page   67: Upper bits of FIFO watermark. Generate interrupt when the FIFO reaches
