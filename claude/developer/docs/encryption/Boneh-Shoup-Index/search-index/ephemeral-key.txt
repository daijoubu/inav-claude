Keyword: ephemeral key
Occurrences: 5
================================================================================

Page  363: again. For example, when sending encrypted email one often picks an ephemeral key and encrypts
Page  363: the email body under this ephemeral key. The ephemeral key is then encrypted and transmitted
Page  363: in the email header. A new ephemeral key is generated for every email.
Page  594: is fast. The idea is as follows: in the o✏ine phase we generate an ephemeral key pair (pk 1 , sk 1 )
Page  752: key encryption scheme E = (G, E, D). As in protocol AKE2, a new, ephemeral key pair for the
